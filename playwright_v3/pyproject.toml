[tool.pytest.ini_options]
# Root pytest configuration. Replaces config/pytest.ini, which lived outside the
# rootdir (so `pytest` never auto-loaded it) and carried another project's
# absolute `pythonpath`.
minversion = "7.0"
testpaths = ["tests"]
addopts = "-ra --strict-markers"
filterwarnings = [
    "ignore::UserWarning",
]
markers = [
    "api: back-end / front-end API tests",
    "ui: Playwright UI tests",
    "smoke: minimal fast subset",
    "regression: full regression subset",
    "req(*ids): the REQ-* requirements this test verifies - checked against docs/source/qa/feature_catalogue.rst by utilities/_devtools/check_traceability.py",
    "tc(*ids): the TC-* test case IDs this test implements - used by utilities/_devtools/generate_test_results_rst.py to join JUnit XML results with test case specifications",
]

# Browser selection: pytest-playwright reads --browser from addopts or the CLI.
# Default is chromium.  To run on Firefox locally:
#   pytest -m ui --browser firefox
# To run on both browsers in one shot:
#   pytest -m ui --browser firefox --browser chromium
# CI overrides this per-job via the workflow matrix (see .github/workflows/ci.yml).
#
# Parallel runs: API tests use -n auto (pytest-xdist); UI tests run -n0 because
# each test gets its own isolated browser context from pytest-playwright - no
# shared driver state to protect.


[tool.coverage.run]
source = ["core", "utilities"]
branch = true
omit = [
    "*/_devtools/*",   # developer-only doc scripts, not test targets
    "*/__init__.py",
]

[tool.coverage.report]
# Floor for the API-only CI run (`-m "not ui"`).
# Page-object code (core/pages/, core/locators/) scores 0 % here because it is
# only exercised by the UI job. Measured value: 59.79 % (API run, v3).
# Raise this as coverage rises — see known_issues KI-14.
fail_under = 59
show_missing = true
exclude_lines = [
    "pragma: no cover",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
]
